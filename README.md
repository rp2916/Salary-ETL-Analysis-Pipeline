# Salary-ETL-Analysis-Pipeline
An end-to-end data pipeline (Python, PostgreSQL, Tableau) that transforms 30k+ raw global survey responses into a clean relational database and interactive compensation dashboard.
